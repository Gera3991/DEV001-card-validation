//const number = document.getElementById(cc-number)


//Se añade evento de click para confirmr 
//document.getElementById("boton_validar").addEventListener("click", creditCardNumber());

//Función para traer el numero de tarjeta con el algoritmo de Luhn
//function creditCardNumber() {
  //let inputValue = Array.from(document.getElementById("cc-number").value.replace(/\D/g, '')); 


  //let luhn = validator.isValid(inputValue);
 // console.log(luhn);
  
    //if (luhn == true){
    //  console.log(checked);
     // checked.classList.remove("hide");
   // } else if (luhn == false) {
   //   console.log(wrong);
   //   wrong.classList.remove("hide");


 // }
  // si el último digito es superior a 0 es invalido
  // si es  igual a 0 es valido

 // if (sum, "si el último digito es superior a 0"){
    return false
 // }
  //else if (sum, "si el digito es igual a 0"){
    return true
  //}
  
  //Return boolean

//},
//}
